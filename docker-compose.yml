x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

services:
  nginx:
    build:
      context: .
      target: nginx
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - frontend
    restart: unless-stopped
    stop_grace_period: 10s
    logging: *default-logging
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 128M

  api:
    build:
      context: .
      target: api
    expose:
      - "8000"
    env_file: .env
    environment:
      VECTOR_STORE__TLS_CA_CERT: /etc/qdrant/ca.crt
    volumes:
      - ./qdrant/certs/server.crt:/etc/qdrant/ca.crt:ro
    depends_on:
      qdrant:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - frontend
      - backend
    restart: unless-stopped
    stop_grace_period: 30s
    logging: *default-logging
    cap_drop:
      - ALL
    tmpfs:
      - /tmp:size=512M,mode=1777
    deploy:
      resources:
        limits:
          cpus: "2.00"
          memory: 4G

  postgres:
    build:
      context: ./postgres
      dockerfile: Dockerfile
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    expose:
      - "5432"
    env_file: .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - ./postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U raguser -d ragdb"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - backend
    restart: unless-stopped
    stop_grace_period: 30s
    logging: *default-logging
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
      - SETUID
      - SETGID
      - DAC_READ_SEARCH
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: 512M

  qdrant:
    build:
      context: ./qdrant
      dockerfile: Dockerfile
    pull_policy: missing
    expose:
      - "6333"
      - "6334"
    environment:
      QDRANT__SERVICE__API_KEY: ${VECTOR_STORE__API_KEY}
    volumes:
      - qdrant_data:/qdrant/storage
      - ./qdrant/qdrant.yaml:/qdrant/config/production.yaml:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sf -k https://127.0.0.1:6333/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - backend
    restart: unless-stopped
    stop_grace_period: 20s
    logging: *default-logging
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: 1G

networks:
  frontend:
    driver: bridge
    internal: false
  backend:
    driver: bridge
    internal: true

volumes:
  qdrant_data:
  postgres_data:
